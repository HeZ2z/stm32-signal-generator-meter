# Include Layout

本目录预留给公共头文件和模块接口定义。

当前建议的接口划分：

- `app/`：应用调度和状态机接口
- `signal_gen/`：`PWM` 输出与参数配置接口
- `ui_ctrl/`：串口命令或按键控制接口
- `display/`：串口输出接口

当前主固件已收口为稳定的 `UART + PWM + 命令` 版本，板级强绑定配置集中在 `board_config.h`，后续修板时优先修改该文件而不是分散改动各模块。
