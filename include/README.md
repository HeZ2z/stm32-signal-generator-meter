# Include Layout

本目录预留给公共头文件和模块接口定义。

当前建议的接口划分：

- `app/`：应用调度和状态机接口
- `signal_gen/`：`PWM` 输出与参数配置接口
- `signal_measure/`：输入捕获与测量结果接口
- `ui_ctrl/`：串口命令或按键控制接口
- `display/`：串口输出接口

在确认准确 `MCU` 型号之前，不建议在这里放入大量与具体芯片强绑定的外设定义。
