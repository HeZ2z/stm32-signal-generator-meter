# Tools

本目录预留给开发辅助脚本。

后续可放入：

- `flash.sh`：烧录脚本
- `serial.sh`：串口监听脚本
- `env-check.sh`：环境检查脚本

当前已提供：

- `env-check.sh`：检查工具链与串口节点
- `serial_monitor.py`：最小串口监听工具

说明：

- 宿主机侧逻辑回归测试入口位于 `tests/run_host_tests.sh`
